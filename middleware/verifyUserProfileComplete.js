export default function ({ store, route, redirect }) {
  /*if (store.getters['user/cognitoUser'] && !store.getters['user/profileComplete']) {
    // If the user is logged in and their profile is incomplete then make sure they complete it
     if (route.name !== 'welcome') {
       redirect("/welcome")
     }
  }*/
  return
}